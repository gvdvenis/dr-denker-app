{
  "name": "dr-denker-app",
  "private": true,
  "version": "1.0.0",
  "description": "Interactive image puzzle web application with zoom, pan, and crossword features",
  "type": "module",
  "scripts": {
    "dev": "npm run optimize-images && vite --host",
    "build": "npm run optimize-images && vite build",
    "preview": "vite preview",
    "optimize-images": "node scripts/optimize-images.js",
    "force-optimize": "node scripts/optimize-images.js --force",
    "generate-icons": "node scripts/generate-pwa-icons.js"
  },
  "dependencies": {
    "@vueuse/core": "^14.1.0",
    "panzoom": "^9.4.3",
    "swiper": "^12.0.3",
    "vue": "^3.4.31",
    "vue-i18n": "^9.14.5",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "sharp": "^0.34.5",
    "terser": "^5.44.1",
    "vite": "^5.3.4",
    "vite-plugin-pwa": "^1.2.0"
  }
}
